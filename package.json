{
  "name": "claude-code-bmad-foundation",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e": "playwright test",
    "e2e:ui": "playwright test --ui",
    "e2e:install": "playwright install"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
